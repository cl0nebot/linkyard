<!DOCTYPE html>
<html>
<head lang="en">
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="robots" content="all, follow" />
  <title><%= content_for?(:title) ? yield(:title) + " | " : "" %>Weekly <%= digest_name(@digest_type) %> Digest</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application'%>
  <%= csrf_meta_tags %>
  <%= auto_discovery_link_tag(:rss, "http://feeds.feedburner.com/digest-#{@digest_type}", { title: "Weekly #{digest_name(@digest_type)} Digest RSS" }) %>
</head>
<body class="digest <%= "#{params[:controller]} #{params[:controller]}-#{params[:action]}" %>">
<div>
  <header>
    <div class="pure-g">
      <div class="pure-u-2-3">
        <h1><%= link_to "Weekly #{digest_name(@digest_type)} Digest", root_path %></h1>
      </div>
      <div class="pure-u-1-3">
        <ul>
          <%= navigation_link_to 'Archive', digests_path %>
          <%= navigation_link_to 'Search', search_digests_path %>
          <%= navigation_link_to 'Contact', contact_path %>
          <%= navigation_link_to 'RSS', "http://feeds.feedburner.com/digest-#{@digest_type}" %>
        </ul>
      </div>
    </div>
  </header>

  <%= content_for?(:content) ? yield(:content) : yield %>

  <div class="social">
    <ul>
      <li class="follow-twitter"><a href="https://twitter.com/intent/user?screen_name=<%= twitter_username(@digest_type) %>">Follow @<%= twitter_username(@digest_type) %></a></li>
    </ul>
  </div>

  <footer>
    &copy; <%= Time.zone.now.year %> <%= link_to "Jakub Chodounský", "https://chodounsky.net" %>
  </footer>
</div>
<script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){ (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o), m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m) })(window,document,'script','//www.google-analytics.com/analytics.js','ga');
  ga('create', '<%= tracking_id(@digest_type) %>', 'auto');
  ga('send', 'pageview');
</script>
</body>
</html>
