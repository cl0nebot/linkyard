<!DOCTYPE html>
<html>
<head>
  <title><%= content_for?(:title) ? yield(:title) + " | " : "" %>Weekly Programming Digest</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application'%>
  <%= csrf_meta_tags %>
</head>
<body class="digest <%= "#{params[:controller]} #{params[:controller]}-#{params[:action]}" %>">
<div>
  <header>
    <div class="pure-g">
      <div class="pure-u-2-3">
        <h1><%= link_to "Weekly Programming Digest", root_path %></h1>
      </div>
      <div class="pure-u-1-3">
        <ul>
          <%= navigation_link_to 'Archive', digests_path %>
          <%= navigation_link_to 'Search', new_link_path %>
          <%= navigation_link_to 'RSS', new_link_path %>
        </ul>
      </div>
    </div>
  </header>

  <% if notice %>
    <div class="notice">
      <p><%= notice %></p>
    </div>
  <% end %>
  <% if alert %>
    <div class="warning">
      <p><%= alert %></p>
    </div>
  <% end %>
  <% if flash[:error] %>
    <div class="error">
      <p><%= flash[:error] %></p>
    </div>
  <% end %>


  <%= content_for?(:content) ? yield(:content) : yield %>

  <footer>
    &copy; <%= Time.zone.now.year %> <%= link_to "Jakub Chodounský", "https://chodounsky.net" %>
  </footer>
</div>
</body>
</html>
