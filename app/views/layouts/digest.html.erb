<!DOCTYPE html>
<html>
<head>
  <title><%= content_for?(:title) ? yield(:title) + " | " : "" %>Weekly Programming Digest</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application'%>
  <%= csrf_meta_tags %>
  <%= auto_discovery_link_tag(:rss, { controller: "digests", action: "feed", format: "rss" }, { title: "Weekly Programming Digest RSS" }) %>
</head>
<body class="digest <%= "#{params[:controller]} #{params[:controller]}-#{params[:action]}" %>">
<div>
  <header>
    <div class="pure-g">
      <div class="pure-u-2-3">
        <h1><%= link_to "Weekly Programming Digest", root_path %></h1>
      </div>
      <div class="pure-u-1-3">
        <ul>
          <%= navigation_link_to 'Archive', digests_path %>
          <%= navigation_link_to 'Search', search_digests_path %>
          <%= navigation_link_to 'RSS', feed_digests_path(format: "rss") %>
        </ul>
      </div>
    </div>
  </header>

  <%= content_for?(:content) ? yield(:content) : yield %>

  <footer>
    &copy; <%= Time.zone.now.year %> <%= link_to "Jakub Chodounský", "https://chodounsky.net" %>
  </footer>
</div>
</body>
</html>
